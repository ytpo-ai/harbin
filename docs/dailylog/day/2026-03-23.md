# 2026-03-23 工作日志

新增 Prompt 批量导入能力，完成 repo-writer 与 save-prompt-template 内置工具接入。

**完成事项**

- 扩展 PromptTemplate Schema，新增 `category/tags/source` 字段并保留历史索引。
- 新增 `repo-writer` 工具定义与后端实现，支持 HTTPS 仓库浅克隆/拉取更新到 `data/repos/**`。
- 新增 `save-prompt-template` 工具定义与处理器，支持单条/批量导入与可选自动发布。
- 更新 ToolService 分发逻辑与 ToolModule 依赖注入，完成新工具运行链路。
- 更新 Agent Tool 功能文档与 guide 缓存文档，补充新能力与代码定位。
- 根据 CR 反馈补充稳健性修正：repo-writer 超限自动清理 clone 目录、跳过 .git 大小统计、save-template 参数契约补全 oneOf/required、PromptRegistryToolHandler 显式写入 source.importedAt。

**影响范围**

- `backend/apps/agents/src/modules/tools/**`
- `backend/apps/agents/src/modules/prompt-registry/**`
- `backend/apps/agents/src/schemas/prompt-template.schema.ts`
- `docs/feature/AGENT_TOOL.md`
- `docs/guide/PROMPT_IMPORT_REPO_WRITER_TOOL.MD`

**关联文档**

- `docs/plan/PROMPT_IMPORT_REPO_WRITER_TOOL_PLAN.md`
- `docs/feature/AGENT_TOOL.md`
- `docs/guide/PROMPT_IMPORT_REPO_WRITER_TOOL.MD`

**补充事项（编排页面）**

- 调整 `frontend/src/pages/PlanDetail.tsx` 与 `frontend/src/pages/Orchestration.tsx` 的任务依赖编辑交互：保留"按钮 + 弹窗"方案，同时将卡片内入口改为紧凑样式（`依赖` 按钮 + `已选 N 项/无依赖` 单行摘要），去除大面积空白框。

**Bug Fix: Native Streaming 工具调用 & 异步任务 Prompt 注入**

- 修复 `agent-task.worker.ts`：异步任务 `runtimeTask.messages` 为空导致 LLM 收不到 user message，Agent 返回泛化回复。将 `task.prompt` 注入为 user message。
- 修复 `native-streaming-agent-executor.engine.ts`：streaming 引擎不支持工具调用循环，LLM 输出的 `<tool_call>` 标签被原样返回。增加 `extractToolCall` 检测，检测到工具调用时 fallback 到 `executeWithToolCalling` 多轮循环。
- 完成 repo-writer → repo-read → save-prompt-template 三工具联动端到端验证，Coder-T Agent 异步任务全流程通过。

**影响范围（Bug Fix）**

- `backend/apps/agents/src/modules/agent-tasks/agent-task.worker.ts`
- `backend/apps/agents/src/modules/agents/executor-engines/native-streaming-agent-executor.engine.ts`

**关联文档（Bug Fix）**

- `docs/issue/fix/2026-03-23-native-streaming-tool-calling-and-task-prompt-injection.md`

**功能优化：工程统计 Step 7（大文件告警条 + 弹框）**

- 完成 `frontend/src/pages/EngineeringStatistics.tsx` 的 Step 7 开发：新增大文件行数阈值常量（1500）、最近统计卡片底部告警条、超限文件明细弹框。
- 告警数据采用纯前端计算：从 `latest.projects[].topLineFiles` 聚合并筛选 `lines >= 1500`，按行数降序展示。
- 弹框表格补齐所属模块映射（Docs / Frontend / Backend(src) / Backend(app)）与字节格式化展示，支持一键关闭。
- 同步更新工程智能功能文档与 guide 缓存文档，补齐该交互与数据来源说明。

**影响范围（功能优化）**

- `frontend/src/pages/EngineeringStatistics.tsx`
- `docs/feature/ENGINEERING_INTELLIGENCE.md`
- `docs/guide/EI_ENGINEERING_STATISTICS_BOARD.MD`

**关联文档（功能优化）**

- `docs/plan/EI_STATISTICS_BOARD_TOPN_OPTIMIZATION_PLAN.md`
- `docs/feature/ENGINEERING_INTELLIGENCE.md`
- `docs/guide/EI_ENGINEERING_STATISTICS_BOARD.MD`

**功能推进：PromptTemplate 绑定 Agent/Skill + save-template 校验增强**

- `save-prompt-template` 增加 handler 层业务校验：`category` 必填且仅允许 `system/recruitment`；当 `category=recruitment` 时强制 `role=<domain>:<persona-role>`。
- `agent.schema.ts`、`agent-skill.schema.ts` 新增 `promptTemplateRef?: { scene, role }`。
- Agent/Skill 创建与更新接口支持设置与清除 `promptTemplateRef`（清除使用 `$unset`）。
- Runtime 注入链路增强：Identity Layer 在 `systemPrompt` 后追加 Agent 绑定 PromptTemplate；Toolset Layer 在技能激活时优先解析 Skill 绑定 PromptTemplate，成功替换 skill content，失败回退原 content。
- 补充单测覆盖上述新行为与校验。

**影响范围（功能推进）**

- `backend/apps/agents/src/modules/tools/prompt-registry-tool-handler.service.ts`
- `backend/apps/agents/src/schemas/agent.schema.ts`
- `backend/apps/agents/src/schemas/agent-skill.schema.ts`
- `backend/apps/agents/src/modules/agents/context/identity-context.builder.ts`
- `backend/apps/agents/src/modules/agents/agent-executor.service.ts`
- `backend/apps/agents/src/modules/agents/agent.service.ts`
- `backend/apps/agents/src/modules/skills/skill.service.ts`

**关联文档（功能推进）**

- `docs/plan/PROMPT_IMPORT_REPO_WRITER_TOOL_PLAN.md`
- `docs/guide/PROMPT_IMPORT_REPO_WRITER_TOOL.MD`
- `docs/feature/AGENT_RUNTIME.md`
- `docs/feature/AGENT_SKILL.md`

**功能开发：Orchestration Service 拆分（Facade + 子 Service）**

- 将 `orchestration.service.ts` 重构为 Facade，保留 Controller 依赖的全部公开方法签名并按职责委派。
- 新增 7 个子 Service：`plan-management`、`task-management`、`plan-execution`、`task-lifecycle`、`plan-stats`、`plan-event-stream`、`orchestration-context`。
- 将原有 4164 行核心实现迁移至 `services/orchestration-legacy.service.ts` 作为兼容过渡层，子 Service 先委派 legacy 保证行为一致。
- 更新 `orchestration.module.ts` Provider 注册，接入 Facade/Legacy/7 子 Service 组合。
- 补充文档沉淀：更新 `docs/feature/ORCHETRATION_TASK.md`，新增 `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`。

**影响范围（Orchestration 拆分）**

- `backend/src/modules/orchestration/orchestration.service.ts`
- `backend/src/modules/orchestration/orchestration.module.ts`
- `backend/src/modules/orchestration/services/orchestration-legacy.service.ts`
- `backend/src/modules/orchestration/services/plan-management.service.ts`
- `backend/src/modules/orchestration/services/task-management.service.ts`
- `backend/src/modules/orchestration/services/plan-execution.service.ts`
- `backend/src/modules/orchestration/services/task-lifecycle.service.ts`
- `backend/src/modules/orchestration/services/plan-stats.service.ts`
- `backend/src/modules/orchestration/services/plan-event-stream.service.ts`
- `backend/src/modules/orchestration/services/orchestration-context.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**关联文档（Orchestration 拆分）**

- `docs/plan/ORCHESTRATION_SERVICE_SPLIT_PLAN.md`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（逐段下沉）**

- 完成 `plan-stats.service.ts` 独立实现：迁移 plan stats 与 plan session 同步相关逻辑，不再通过 `any` 调用 legacy 私有方法。
- 完成 `plan-event-stream.service.ts` 独立实现：迁移 plan SSE stream 管理、快照推送与事件分发能力。
- 完成 `orchestration-context.service.ts` 独立实现：迁移依赖上下文拼接、增强 prompt 构建、领域上下文推断、EI 需求状态 best-effort 回写。
- 按拆分计划清理 legacy 旧校验方法：删除 research/review/email 的历史校验/提取实现，统一走 `TaskOutputValidationService` 与 `TaskClassificationService`。
- 重新执行后端 lint/build，均通过。

**影响范围（Orchestration 拆分第 2 阶段）**

- `backend/src/modules/orchestration/services/plan-stats.service.ts`
- `backend/src/modules/orchestration/services/plan-event-stream.service.ts`
- `backend/src/modules/orchestration/services/orchestration-context.service.ts`
- `backend/src/modules/orchestration/services/orchestration-legacy.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（继续下沉）**

- `plan-management.service.ts` 新增独立实现（`listPlans/getPlanById/updatePlan/deletePlan`），逐步摆脱 legacy 直接依赖。
- `plan-execution.service.ts` 新增独立实现（`listPlanRuns/getLatestPlanRun/getRunById/listRunTasks`），执行链路入口暂保持 legacy 委派。
- `task-management.service.ts` 新增独立实现 `listTasksByPlan`，其余编辑能力继续走 legacy。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration 拆分第 2 阶段继续）**

- `backend/src/modules/orchestration/services/plan-management.service.ts`
- `backend/src/modules/orchestration/services/plan-execution.service.ts`
- `backend/src/modules/orchestration/services/task-management.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（任务编辑链路下沉）**

- `task-management.service.ts` 完成任务模板编辑链路独立实现：`add/remove/update/reorder/batch/duplicate/updateDraft`。
- 同步迁移任务编辑相关校验与工具方法：计划可编辑状态校验、依赖归属校验、Kahn 循环依赖检测、assignment/status 归一化。
- 任务编辑后的 PlanSession 同步与 Plan SSE 事件推送改为直接依赖 `PlanStatsService` + `PlanEventStreamService`。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration 任务编辑链路下沉）**

- `backend/src/modules/orchestration/services/task-management.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（任务运行态链路下沉）**

- `task-lifecycle.service.ts` 完成 `reassignTask/completeHumanTask/retryTask` 独立实现，迁移 tier 治理校验、run 统计回写、task lifecycle 事件推送。
- `debugTaskStep/executeStandaloneTask` 先保留 legacy 委派，避免在执行引擎尚未完全下沉时引入行为偏差。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration 任务运行态链路下沉）**

- `backend/src/modules/orchestration/services/task-lifecycle.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（计划生成管线下沉）**

- `plan-management.service.ts` 完成计划生成与重规划主链路独立实现：`createPlanFromPrompt/generatePlanTasksAsync/replanPlan/replanPlanAsync`。
- 迁移建计划/重规划过程中的状态推进与事件发布：`PlanStatsService` + `PlanEventStreamService`；自动运行触发改为通过 `PlanExecutionService` 协调。
- 补回计划更新时“运行中不可编辑”校验，避免并发运行态下的配置写入。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration 计划生成管线下沉）**

- `backend/src/modules/orchestration/services/plan-management.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（执行入口下沉）**

- `plan-execution.service.ts` 完成 `runPlan/runPlanAsync` 独立实现，迁移异步运行防重与失败回写处理。
- 当前执行层仅保留 `executePlanRun` 对 legacy 的委派，便于后续一次性迁移执行引擎主干。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration 执行入口下沉）**

- `backend/src/modules/orchestration/services/plan-execution.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（debug/standalone 入口下沉）**

- `task-lifecycle.service.ts` 完成 `debugTaskStep/executeStandaloneTask` 入口迁移，调试参数预处理、依赖检查、状态回写在新服务完成。
- 为保证行为一致，任务节点实际执行仍复用 legacy 的执行引擎私有能力（`executeTaskNode`）。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration debug/standalone 入口下沉）**

- `backend/src/modules/orchestration/services/task-lifecycle.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（executePlanRun 下沉）**

- `plan-execution.service.ts` 完成 `executePlanRun` 独立实现，迁移 run 实例创建、run task 快照生成、调度结果回写、SSE 事件发布、requirement 状态同步。
- `executeRunTasks` 循环迁移到 `plan-execution`，run task 实际执行仍复用 legacy 私有 `executeRunTaskNode`，保持运行行为一致。
- 本轮后 7 个子 Service 对外 API 均已独立化，legacy 主要承担执行引擎私有复用层角色。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration executePlanRun 下沉）**

- `backend/src/modules/orchestration/services/plan-execution.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（执行引擎适配层）**

- 新增 `orchestration-execution-engine.service.ts`，抽离 task node / run task node 执行入口适配，统一复用 legacy 私有执行能力。
- `plan-execution.service.ts` 与 `task-lifecycle.service.ts` 改为依赖 execution engine 适配层，不再直接调用 legacy 私有方法。
- 修复 `task-lifecycle.retryTask` 中 run-task 分支对 legacy 公共接口的回退，改为独立执行与 run 状态回写。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration 执行引擎适配层）**

- `backend/src/modules/orchestration/services/orchestration-execution-engine.service.ts`
- `backend/src/modules/orchestration/services/plan-execution.service.ts`
- `backend/src/modules/orchestration/services/task-lifecycle.service.ts`
- `backend/src/modules/orchestration/orchestration.module.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（run-task 执行主流程下沉）**

- `orchestration-execution-engine.service.ts` 内部迁移 `executeRunTaskNode` 主流程，接管 run task 的执行开始/完成/失败/等待人工状态推进。
- 同步迁移 async agent task 等待链路（SSE 优先 + polling fallback）与输出提取逻辑，减少对 legacy 执行私有函数依赖。
- `plan-execution`、`task-lifecycle` 继续通过 execution engine 调用执行能力，调用关系保持稳定。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration run-task 执行主流程下沉）**

- `backend/src/modules/orchestration/services/orchestration-execution-engine.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（task-node 执行主流程下沉）**

- `orchestration-execution-engine.service.ts` 完成 `executeTaskNode` 主流程迁移，接管 task 的执行开始/完成/失败/等待人工状态推进及 PlanSession 回写。
- 同步迁移 task 级 async agent task 等待、runtime task type 判定、research/review/external/code-proof 校验逻辑。
- 本轮后 orchestration 子服务内已无对 `OrchestrationLegacyService` 的直接依赖（执行能力统一经 execution engine 服务注入）。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration task-node 执行主流程下沉）**

- `backend/src/modules/orchestration/services/orchestration-execution-engine.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（legacy 运行时解绑）**

- `orchestration.module.ts` 移除 `OrchestrationLegacyService` Provider 注册，运行时依赖链完全切换到拆分后的子 Service + execution engine。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration legacy 运行时解绑）**

- `backend/src/modules/orchestration/orchestration.module.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

**功能开发：Orchestration Service 拆分第 2 阶段（legacy 文件清理）**

- 删除 `backend/src/modules/orchestration/services/orchestration-legacy.service.ts` 遗留实现文件。
- 执行引擎能力已全部切换到 `orchestration-execution-engine.service.ts`（`executeTaskNode/executeRunTaskNode`）。
- 再次执行后端 lint/build，均通过。

**影响范围（Orchestration legacy 文件清理）**

- `backend/src/modules/orchestration/services/orchestration-legacy.service.ts`（删除）
- `backend/src/modules/orchestration/services/orchestration-execution-engine.service.ts`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

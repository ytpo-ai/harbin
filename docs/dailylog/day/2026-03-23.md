# 2026-03-23 工作日志

新增 Prompt 批量导入能力，完成 repo-writer 与 save-prompt-template 内置工具接入。

**完成事项**

- 扩展 PromptTemplate Schema，新增 `category/tags/source` 字段并保留历史索引。
- 新增 `repo-writer` 工具定义与后端实现，支持 HTTPS 仓库浅克隆/拉取更新到 `data/repos/**`。
- 新增 `save-prompt-template` 工具定义与处理器，支持单条/批量导入与可选自动发布。
- 更新 ToolService 分发逻辑与 ToolModule 依赖注入，完成新工具运行链路。
- 更新 Agent Tool 功能文档与 guide 缓存文档，补充新能力与代码定位。

**影响范围**

- `backend/apps/agents/src/modules/tools/**`
- `backend/apps/agents/src/modules/prompt-registry/**`
- `backend/apps/agents/src/schemas/prompt-template.schema.ts`
- `docs/feature/AGENT_TOOL.md`
- `docs/guide/PROMPT_IMPORT_REPO_WRITER_TOOL.MD`

**关联文档**

- `docs/plan/PROMPT_IMPORT_REPO_WRITER_TOOL_PLAN.md`
- `docs/feature/AGENT_TOOL.md`
- `docs/guide/PROMPT_IMPORT_REPO_WRITER_TOOL.MD`

**补充事项（编排页面）**

- 调整 `frontend/src/pages/PlanDetail.tsx` 与 `frontend/src/pages/Orchestration.tsx` 的任务依赖编辑交互：保留“按钮 + 弹窗”方案，同时将卡片内入口改为紧凑样式（`依赖` 按钮 + `已选 N 项/无依赖` 单行摘要），去除大面积空白框。

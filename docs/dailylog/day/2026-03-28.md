# 2026-03-28 工作日志

Orchestration 任务类型链路调整：任务生成阶段落盘 `taskType`，pre_execute 基于 `taskType` 推断 `runtimeTaskType`，并移除关键词分类分支。

**完成事项**

- 新增编排改造计划文档：`docs/plan/ORCHESTRATION_TASKTYPE_DEFAULT_RUNTIME_MAPPING_PLAN.MD`。
- 调整 planner 输出契约：`generate-next` JSON 新增 `task.taskType`（合法值：`general/research/development.plan/development.exec/development.review`），缺失时兜底 `general`。
- 扩展任务数据模型：`orchestration_tasks` 与 `orchestration_run_tasks` 新增 `taskType` 字段，并在任务创建/重设计/运行快照路径写入。
- 调整 runtime 推断优先级：`existingRuntimeTaskType > taskType > domain fallback`，`phasePreExecute` 与增量执行路径均传入 `taskType`。
- 删除 `orchestration-context.service.ts` 中关键词判断函数：`isPlanningLikeTask`、`isResearchLikeTask`、`isCodeReviewLikeTask`、`isCodeLikeTask`。
- 更新相关测试与文档：`orchestration-context-infer-tasktype` 用例改为 `taskType` 驱动；更新 `docs/guide/ORCHESTRATION_TASK_TYPE_ROUTING_CHAIN.MD`、`docs/feature/ORCHETRATION_TASK.md`、`docs/skill/orchestration-runtime-tasktype-selection.md`。

**影响范围**

- 后端：`backend/src/modules/orchestration/planner.service.ts`。
- 后端：`backend/src/modules/orchestration/services/incremental-planning.service.ts`。
- 后端：`backend/src/modules/orchestration/services/orchestration-step-dispatcher.service.ts`。
- 后端：`backend/src/modules/orchestration/services/orchestration-context.service.ts`。
- Schema：`backend/src/shared/schemas/orchestration-task.schema.ts`、`backend/src/shared/schemas/orchestration-run-task.schema.ts`。
- 测试：`backend/test/orchestration/orchestration-context-infer-tasktype.spec.ts`、`backend/src/modules/orchestration/services/orchestration-context.service.spec.ts`。
- 文档：`docs/guide/ORCHESTRATION_TASK_TYPE_ROUTING_CHAIN.MD`、`docs/feature/ORCHETRATION_TASK.md`、`docs/skill/orchestration-runtime-tasktype-selection.md`。

**关联文档**

- `docs/plan/ORCHESTRATION_TASKTYPE_DEFAULT_RUNTIME_MAPPING_PLAN.MD`
- `docs/guide/ORCHESTRATION_TASK_TYPE_ROUTING_CHAIN.MD`
- `docs/feature/ORCHETRATION_TASK.md`

---

计划编排列表复制创建能力：列表项新增“复制并新建”按钮，点击后打开创建弹窗并回填当前计划参数。

**完成事项**

- 新增计划文档：`docs/plan/ORCHESTRATION_PLAN_LIST_COPY_TO_CREATE_PLAN.MD`。
- 列表操作区新增“复制并新建”入口，支持从计划列表直接发起复制创建。
- 页面编排层复用创建弹窗状态与字段，复制时回填 `title/prompt/mode/runMode/domainType/plannerAgentId`。
- 复制默认将标题追加“ - 副本”，并关闭“创建并生成任务”开关，降低误触风险。
- 更新功能文档，补充计划列表“复制并新建”能力说明。

**影响范围**

- 前端页面：`frontend/src/pages/orchestration/index.tsx`。
- 前端组件：`frontend/src/pages/orchestration/components/PlanListTable.tsx`。
- 文档：`docs/plan/ORCHESTRATION_PLAN_LIST_COPY_TO_CREATE_PLAN.MD`、`docs/feature/ORCHETRATION_TASK.md`。

**关联文档**

- `docs/plan/ORCHESTRATION_PLAN_LIST_COPY_TO_CREATE_PLAN.MD`
- `docs/feature/ORCHETRATION_TASK.md`

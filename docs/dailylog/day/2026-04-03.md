# 2026-04-03 工作日志

清理 Skill 冗余字段 `planningRules`，下线对应解析/同步/脚本链路并完成历史数据迁移。

**完成事项**

- 移除 Skill Schema 的 `planningRules` 与 `PlanningRule` 类型定义，收敛 skill 数据模型。
- 移除 skill 文档加载器对 frontmatter `planningRules` 的解析与归一化流程。
- 移除 skill 文档同步入库 payload 和变更比对中的 `planningRules` 分支。
- 删除遗留 seed 脚本：`backend/scripts/seed/skill-planning-rules.ts`、`backend/scripts/seed/seed-skill-planning-rules.ts`。
- 新增迁移脚本 `backend/scripts/migrate/unset-skill-planning-rules.ts`，并在 `backend/package.json` 注册 `migrate:unset-skill-planning-rules`。
- 已执行迁移：首次 `matched=12 modified=12`，复验 dry-run `matched=0`。
- 更新文档：`docs/guide/SKILL_LOAD.md`、`docs/plan/SKILL_DOC_SYNC_TO_DB_PLAN.md`，并新增本次计划文档 `docs/plan/SKILL_PLANNING_RULES_CLEANUP_PLAN.md`。

**影响范围**

- Agent Skill 文档同步链路（frontmatter 字段映射与变更检测）。
- Skill 数据模型与 API 返回结构（移除 `planningRules` 字段）。
- 运维脚本（planningRules seed 下线，新增清理迁移脚本）。

**关联文档**

- `docs/plan/SKILL_PLANNING_RULES_CLEANUP_PLAN.md`
- `docs/guide/SKILL_LOAD.md`
- `docs/plan/SKILL_DOC_SYNC_TO_DB_PLAN.md`

# 2026-04-29 工作日志

Discussion Space REQ-005：讨论详情页布局优化与沉淀历史可见性增强。

**完成事项**

- 新增需求文档：`docs/requirement/DISCUSSION_SPACE_REQ-005_LAYOUT_AND_SEDIMENT_HISTORY.md`
- 更新计划与追溯：`docs/plan/DISCUSSION_SPACE_PLAN.md`、`docs/feature/DISCUSSION_SPACE.md`
- 讨论详情页宽度优化：针对 `/discussions/:spaceId` 放宽 `Layout` 容器宽度限制，提升中央讨论区可视空间
- 三栏比例优化：`DiscussionDetail` 调整左右侧栏宽度，增强中间消息区阅读体验
- 文档沉淀历史能力：后端新增 `GET /discussions/:spaceId/sediment/history`，前端服务与沉淀面板接入历史版本查看
- “立即沉淀”执行链路升级：后端优先调用会议助手 Agent runtime 进行沉淀，失败时自动回退模板生成
- 沉淀执行模式升级为 SSE：`/sediment/generate` 改为返回任务，新增沉淀任务事件流接口，前端改为订阅任务状态并自动刷新结果
- 沉淀交互补充标题输入：点击“立即沉淀”需输入标题，后端将标题写入历史并作为会议助手 Agent 沉淀指令与文档标题
- UI 可解释性增强：在“最新沉淀文档”区域增加说明文案，明确“立即沉淀”后的查看位置
- 完成构建验证：`frontend` 执行 `npm run build`，`backend` 执行 `pnpm run build:agents`

**影响范围**

- 前端：`frontend/src/components/Layout.tsx`、`frontend/src/pages/discussions/DiscussionDetail.tsx`、`frontend/src/components/discussion/SedimentPanel.tsx`、`frontend/src/services/discussionService.ts`
- 后端：`backend/src/modules/discussions/discussion.controller.ts`、`backend/src/modules/discussions/discussion.types.ts`、`backend/src/modules/discussions/services/discussion-sediment.service.ts`
- 文档：`docs/plan/`、`docs/requirement/`、`docs/feature/`、`docs/dailylog/day/`

**关联文档**

- `docs/plan/DISCUSSION_SPACE_PLAN.md`
- `docs/requirement/DISCUSSION_SPACE_REQ-005_LAYOUT_AND_SEDIMENT_HISTORY.md`
- `docs/feature/DISCUSSION_SPACE.md`

---

Discussion Space REQ-006：沉淀历史删除能力（软删除 + 权限校验 + 删除交互）。

**完成事项**

- 新增需求文档：`docs/requirement/DISCUSSION_SPACE_REQ-006_SEDIMENT_HISTORY_DELETE.md`
- 更新计划与追溯：`docs/plan/DISCUSSION_SPACE_PLAN.md`、`docs/feature/DISCUSSION_SPACE.md`
- 后端新增删除接口：`DELETE /discussions/:spaceId/sediment/history/:historyId`
- 删除策略改为软删除：`sedimentHistory` 新增 `isDeleted/deletedAt/deletedBy` 元数据，历史查询自动过滤已删除记录
- 权限控制：仅讨论空间创建者可删除沉淀历史
- 最新沉淀回填：删除当前最新版本后，自动回退到未删除的最新沉淀内容
- 前端沉淀面板新增删除入口与确认交互，删除后自动刷新 latest/history
- 完成验证：`backend` 执行 `pnpm test -- discussion-sediment.service.spec.ts`、`pnpm run build:agents`，`frontend` 执行 `pnpm build`

**影响范围**

- 后端：`backend/src/shared/schemas/discussion-space.schema.ts`、`backend/src/modules/discussions/discussion.controller.ts`、`backend/src/modules/discussions/discussion.types.ts`、`backend/src/modules/discussions/services/discussion-sediment.service.ts`
- 前端：`frontend/src/components/discussion/SedimentPanel.tsx`、`frontend/src/pages/discussions/DiscussionDetail.tsx`、`frontend/src/services/discussionService.ts`
- 文档：`docs/plan/`、`docs/requirement/`、`docs/feature/`、`docs/dailylog/day/`

**关联文档**

- `docs/plan/DISCUSSION_SPACE_PLAN.md`
- `docs/requirement/DISCUSSION_SPACE_REQ-006_SEDIMENT_HISTORY_DELETE.md`
- `docs/feature/DISCUSSION_SPACE.md`

---

Discussion Space REQ-007：讨论线删除能力（非主线保护 + 级联删除 + 前端回退）。

**完成事项**

- 新增需求文档：`docs/requirement/DISCUSSION_SPACE_REQ-007_THREAD_DELETE_CAPABILITY.md`
- 更新计划与追溯：`docs/plan/DISCUSSION_SPACE_PLAN.md`、`docs/feature/DISCUSSION_SPACE.md`
- 后端新增讨论线删除接口：`DELETE /discussions/:spaceId/threads/:threadId`
- 删除策略：禁止删除主讨论线；删除分支时级联删除子讨论线、消息与知识条目
- 一致性维护：删除后同步维护父线 `childThreadIds`，并回收空间统计 `totalThreads/totalMessages/totalKnowledgeEntries`
- 前端左侧讨论线新增“删除”入口（仅非主线显示），增加不可恢复确认提示
- 前端状态回退：当前选中线被删除时，自动切回主讨论线并刷新详情/消息/知识视图
- 完成验证：`backend` 执行 `npm test -- discussion-thread.service.spec.ts`，`frontend` 执行 `npm run build`

**影响范围**

- 后端：`backend/src/modules/discussions/discussion.controller.ts`、`backend/src/modules/discussions/discussion.types.ts`、`backend/src/modules/discussions/services/discussion-thread.service.ts`、`backend/src/modules/discussions/services/discussion-thread.service.spec.ts`
- 前端：`frontend/src/components/discussion/ThreadTree.tsx`、`frontend/src/pages/discussions/DiscussionDetail.tsx`、`frontend/src/services/discussionService.ts`
- 文档：`docs/plan/`、`docs/requirement/`、`docs/feature/`、`docs/dailylog/day/`

**关联文档**

- `docs/plan/DISCUSSION_SPACE_PLAN.md`
- `docs/requirement/DISCUSSION_SPACE_REQ-007_THREAD_DELETE_CAPABILITY.md`
- `docs/feature/DISCUSSION_SPACE.md`

---

Discussion Space：分叉线程自动注入来源消息上下文（branch_context）。

**完成事项**

- 后端分叉链路增强：从消息分叉创建线程后，自动写入一条系统消息，包含分叉来源线程名、来源消息序号与来源消息内容
- 新增 `createBranchContextMessage` 逻辑，消息类型为 `branch_context`，用于让新分支开场即具备可见上下文
- 统计同步：注入的上下文消息会同步更新线程消息数与空间总消息数
- 补充单测：新增 `buildBranchContextMessageContent` 文案构造测试，覆盖正常和空内容场景
- 完成验证：`backend` 执行 `npm test -- discussion-message.service.spec.ts`、`npm run build:agents`

**影响范围**

- 后端：`backend/src/modules/discussions/discussion.controller.ts`、`backend/src/modules/discussions/services/discussion-message.service.ts`、`backend/src/modules/discussions/services/discussion-thread.service.ts`、`backend/src/modules/discussions/services/discussion-message.service.spec.ts`
- 文档：`docs/feature/DISCUSSION_SPACE.md`、`docs/dailylog/day/2026-04-29.md`

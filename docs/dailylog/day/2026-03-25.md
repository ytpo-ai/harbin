# 2026-03-25 工作日志

定时计划能力优化：定时器收敛为编排计划触发器，创建时不再指定 Agent。

**完成事项**

- 新增并落盘本次执行计划文档：`docs/plan/ORCHESTRATION_SCHEDULER_PLAN_BINDING_OPTIMIZATION_PLAN.md`。
- 后端 Scheduler DTO 改造：创建/更新定时计划改为围绕 `planId`，移除创建入参中的 `target`。
- 后端 Scheduler Service 改造：创建/更新时强制校验 `planId` 与计划存在性，并基于绑定计划回填兼容 `target` 字段。
- 前端 Scheduler 页面改造：新建/编辑弹窗移除 Agent 选择与 prompt 输入，改为必选“绑定编排计划”。
- 前端 Scheduler 列表/详情增加“绑定计划”信息展示，保留跳转到计划详情的能力。
- Agent 侧编排工具创建 schedule 入参同步更新，移除旧的 `target/input` 透传，避免校验拦截。
- 功能文档 `docs/feature/ORCHETRATION_SCHEDULER.md` 同步更新为“定时器绑定 plan”语义。
- 修复 PlanDetail 重新编排交互：避免 replan 异步受理阶段因立即回源导致旧任务列表回填，保障“先清空后生成”的可视行为。
- 修复 replan 异步受理假成功：后端在 `replanPlanAsync` 受理前补齐运行态/锁定态/prompt 校验，避免前端清空后刷新又回显旧任务。
- 调整编排页编辑策略：按调试诉求放开 `drafting` 状态下的任务重分配/调试能力，前后端门禁统一允许 `drafting`。
- 新增 fix 记录：`docs/issue/fix/2026-03-25-orchestration-replan-task-list-not-cleared.md`。

**影响范围**

- `backend/src/modules/orchestration/scheduler/dto/index.ts`
- `backend/src/modules/orchestration/scheduler/scheduler.service.ts`
- `backend/apps/agents/src/modules/tools/builtin/orchestration-tool-handler.service.ts`
- `frontend/src/services/schedulerService.ts`
- `frontend/src/pages/Scheduler.tsx`
- `frontend/src/hooks/useReplanMutation.ts`
- `docs/feature/ORCHETRATION_SCHEDULER.md`
- `docs/issue/fix/2026-03-25-orchestration-replan-task-list-not-cleared.md`

**验证记录**

- `backend`: `npm run build` 通过
- `frontend`: `npm run build` 通过

**关联文档**

- `docs/plan/ORCHESTRATION_SCHEDULER_PLAN_BINDING_OPTIMIZATION_PLAN.md`
- `docs/feature/ORCHETRATION_SCHEDULER.md`

# 2026-03-24 工作日志

修复增量编排引擎两个关键问题：agent 分配错误和 task type 误判。

**完成事项**

- 修复 Agent Manifest 未暴露 id 导致 planner 无法正确指定 agentId 的问题（manifest 格式加入 `id=ObjectId`）
- 新增 `defaultTaskType` 字段到 Plan schema 和 CreatePlanFromPromptDto，支持创建 Plan 时指定默认任务类型
- planner prompt JSON schema 新增可选 `taskType` 字段，planner 可根据任务性质指定执行类型
- `createTaskFromPlannerOutput` 增加 runtimeTaskType 三层解析：planner 指定 > plan.defaultTaskType > 'general'
- 完成增量编排 E2E 测试，15/16 设计点验证通过（任务创建+执行 PASS，任务合并待验证）

**影响范围**

- `backend/src/modules/orchestration/services/planning-context.service.ts` — manifest 格式变更
- `backend/src/shared/schemas/orchestration-plan.schema.ts` — 新增 defaultTaskType 字段
- `backend/src/modules/orchestration/dto/index.ts` — 3 个 DTO 新增 defaultTaskType
- `backend/src/modules/orchestration/planner.service.ts` — GenerateNextTaskResult 接口新增 taskType + normalizeTaskType
- `backend/src/modules/orchestration/services/incremental-planning.service.ts` — task 创建时持久化 runtimeTaskType + 诊断字段
- `backend/src/modules/orchestration/services/plan-management.service.ts` — create/update/replan 持久化 defaultTaskType

**关联文档**

- `docs/technical/ORCHESTRATION_INCREMENTAL_PLANNING_TECHNICAL_DESIGN.md` — 第 11 节 runtimeTaskType 解析链路
- `docs/guide/TEST_GUIDELINE.MD` — 第 8-9 节已知问题修复记录与 16 设计点验证状态

---

补充：系统守护机制修复（Plan 3）

**完成事项**

- 增量编排新增 Planner 选人守护模式：`PLANNER_AGENT_SELECTION_MODE=trust|verify|override`（默认 verify）
- `ExecutorSelectionService` 新增 `validateAgentToolFit()`，在 Planner 指派后执行 Agent-Task 工具匹配二次校验
- `IncrementalPlanningService` 支持 `action=redesign` 原地改写失败任务并重跑，不再只追加新任务
- `generationState` 新增 `totalFailures`，`generationConfig` 新增 `maxTotalFailures`，熔断改为“连续失败 + 累计失败”双条件
- Planner 上下文失败任务补充 `agentId + agentTools`，Prompt 输出新增 redesign 指令与 JSON schema 扩展
- 新增单测 `backend/test/orchestration/planner.incremental-redesign.spec.ts` 覆盖 redesign 解析与失败上下文提示
- 根据 code review 追加守护修复：general 任务工具推断兜底、redesignTaskId ObjectId 校验、失败上下文工具名可读化（name+id）

**影响范围**

- `backend/src/modules/orchestration/services/incremental-planning.service.ts`
- `backend/src/modules/orchestration/services/executor-selection.service.ts`
- `backend/src/modules/orchestration/planner.service.ts`
- `backend/src/modules/orchestration/services/plan-management.service.ts`
- `backend/src/shared/schemas/orchestration-plan.schema.ts`
- `backend/.env.example`

**关联文档**

- `docs/plan/ORCHESTRATION_INCREMENTAL_PLANNING_FAILOVER_FIX_PLAN.md`
- `docs/technical/ORCHESTRATION_INCREMENTAL_PLANNING_TECHNICAL_DESIGN.md`（第 12 节）
- `docs/feature/ORCHETRATION_TASK.md`

---

补充：Plan 2 执行输出校验修复（Agent inability 拦截）

**完成事项**

- `TaskOutputValidationService` 新增 `validateGeneralOutput()`，统一拦截空输出与 inability 信号（含 `TASK_INABILITY:`）。
- `validateResearchOutput()` inability 检测改为正则词库，扩展“我这边无法 / 当前会话没有 / 没有接入 / 缺少.*工具”等模式。
- `OrchestrationExecutionEngineService` 在 task/run 成功路径前注入通用输出校验，避免 general/development 任务“虚假完成”。
- development 输出校验支持 `CODE_VALIDATION_MODE=warn|strict`；strict 模式下缺少执行证明直接失败。
- `OrchestrationContextService.buildTaskDescription()` 增加 Task Output Contract，要求 inability 必须以 `TASK_INABILITY: <reason>` 开头。
- 新增/更新单测：`backend/test/orchestration/task-output-validation.service.spec.ts` 覆盖通用 inability 与 research 扩展词库。

**影响范围**

- `backend/src/modules/orchestration/services/task-output-validation.service.ts`
- `backend/src/modules/orchestration/services/orchestration-execution-engine.service.ts`
- `backend/src/modules/orchestration/services/orchestration-context.service.ts`
- `backend/test/orchestration/task-output-validation.service.spec.ts`
- `backend/.env.example`

**关联文档**

- `docs/plan/ORCHESTRATION_TASK_OUTPUT_VALIDATION_FIX_PLAN.md`
- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

---

补充：Prompt Registry 读工具与 Agent/Skill Prompt 绑定前端接入

**完成事项**

- 后端新增 Prompt Registry 读工具：
  - `builtin.sys-mg.mcp.prompt-registry.list-templates`（摘要列表，不含 content）
  - `builtin.sys-mg.mcp.prompt-registry.get-template`（完整内容）
- Prompt Registry 工具分发链路补齐：`definitions -> catalog -> handler -> dispatch`。
- 前端新增 `PromptTemplateRefPicker` 组件，并接入：
  - Agent 创建/编辑页（`systemPrompt` 上方，支持绑定/预览/清除）
  - Skill 详情抽屉（`content` 上方，绑定后提示 fallback 语义）
- 更新计划文档 `docs/plan/PROMPT_IMPORT_REPO_WRITER_TOOL_PLAN.md` 的第 6 节 checklist 状态。

**影响范围**

- `backend/apps/agents/src/modules/tools/builtin-tool-definitions.ts`
- `backend/apps/agents/src/modules/tools/builtin-tool-catalog.ts`
- `backend/apps/agents/src/modules/tools/prompt-registry-tool-handler.service.ts`
- `backend/apps/agents/src/modules/tools/tool.service.ts`
- `frontend/src/components/PromptTemplateRefPicker.tsx`
- `frontend/src/pages/Agents.tsx`
- `frontend/src/pages/Skills.tsx`
- `frontend/src/types/index.ts`

**验证记录**

- `backend`: `npm test -- prompt-registry-tool-handler.service.spec.ts` 通过
- `backend`: `npm run build:agents` 通过
- `frontend`: `npm run build` 通过
- `docs/feature/ORCHETRATION_TASK.md`

---

补充：计划编排历史文档弃用标记

**完成事项**

- 检索并核对计划编排相关 guide 文档与当前实现差异（Facade + 子 Service + execution engine）。
- 将以下历史分析文档标记为“已弃用”，并补充替代阅读入口：
  - `docs/guide/ORCHESTRATION_PLAN.MD`
  - `docs/guide/PLANER&PROMPT.MD`
  - `docs/guide/EXECUTOR.MD`

**影响范围**

- `docs/guide/ORCHESTRATION_PLAN.MD`
- `docs/guide/PLANER&PROMPT.MD`
- `docs/guide/EXECUTOR.MD`

**关联文档**

- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/feature/ORCHETRATION_SCHEDULER.md`

---

补充：编排文档状态标签调整

**完成事项**

- 根据文档分层规则，`docs/guide` 下历史编排分析文档从“已弃用”改为 `[TODO 重新整理]`：
  - `docs/guide/ORCHESTRATION_PLAN.MD`
  - `docs/guide/PLANER&PROMPT.MD`
  - `docs/guide/EXECUTOR.MD`
- 将 `docs/development`、`docs/technical`、`docs/plan` 下名称包含 `ORCHESTRATION` 的历史文档统一标记为 `[已弃用]`（共 50 篇）。

**影响范围**

- `docs/guide/ORCHESTRATION_PLAN.MD`
- `docs/guide/PLANER&PROMPT.MD`
- `docs/guide/EXECUTOR.MD`
- `docs/development/*ORCHESTRATION*.md`
- `docs/technical/*ORCHESTRATION*.md`
- `docs/plan/*ORCHESTRATION*.md`

**关联文档**

- `docs/guide/ORCHESTRATION_SERVICE_SPLIT_RUNTIME.MD`

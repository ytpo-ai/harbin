# 2026-07-02 工作日志

DeepSeek 模型接入修复：将 `deepseek` provider 接入 AIV2 OpenAI-compatible 调用链，修复“API调用暂未实现”的占位响应问题。

**完成事项**

- `ModelService` 新增 deepseek 走 AIV2 的路由判定
- `AIV2Provider` 新增 deepseek 构造分支，接入 `DEEPSEEK_API_KEY`
- `AIV2Provider` 增加 DeepSeek 缺失 Key 的显式报错，避免误导为 `OPENAI_API_KEY`
- DeepSeek 默认 base URL 对齐官方文档为 `https://api.deepseek.com`（保留 `DEEPSEEK_BASE_URL` 可覆盖）
- 模型测试逻辑调整：自定义 Key 超时时不再回退系统 Key，避免混淆报错
- 模型测试超时调整：DeepSeek 默认超时提升至 60s（可用 `DEEPSEEK_MODEL_TEST_TIMEOUT_MS` 覆盖）
- 运行 `pnpm run build:agents` 完成编译验证

**影响范围**

- 后端：`backend/apps/agents/src/modules/models/model.service.ts`
- 后端：`backend/libs/models/src/aiv2-provider.ts`
- 后端：`backend/apps/agents/src/modules/agents/agent-executor.service.ts`

Agent 列表筛选持久化优化：将 Agent 管理页 Tier/项目筛选项持久化到 `localStorage`，返回列表与刷新后保持用户上次选择。

**完成事项**

- 新增 `agents:list:filters:v1` 本地存储键，页面初始化恢复筛选值
- 增加 Tier allowlist 与 projectId 容错解析，脏值自动回退默认筛选
- 筛选变化时自动写回 `localStorage`
- 完成 `pnpm run build` 前端编译验证
- 同步新增 plan/requirement，并更新 feature 追溯表

**影响范围**

- 前端：`frontend/src/pages/Agents.tsx`
- 文档：`docs/plan/AGENT_MG_AGENT_LIST_FILTER_PERSIST_PLAN.md`
- 文档：`docs/requirement/AGENT_MG_REQ-002_AGENT_LIST_FILTER_PERSIST.md`
- 文档：`docs/feature/AGENT_MG.md`

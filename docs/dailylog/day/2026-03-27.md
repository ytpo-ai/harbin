# 2026-03-27 工作日志

Orchestration 事件驱动编排收口：补齐旧循环移除、replan session 生命周期、并发与阶段降级测试缺口。

**完成事项**

- 移除 `IncrementalPlanningService` 中的 `for(;;)` 无限循环，改为 `setImmediate` 递进推进，避免服务内阻塞式持续循环。
- `PlanManagementService.replanPlan` 补齐 planner session 生命周期：replan 前归档旧 `plannerSessionId`，并显式创建新 replan session 写回 `generationState.plannerSessionId`。
- 补充 Dispatcher 关键测试：内部直调 + 外部事件并发冲突只允许一次推进、B 阶段 `allowExecute=false` 阻断 C、D 阶段 `retry/redesign` 决策分支、D 阶段异常降级 `stop`。
- 新增 PlanManagement 回归测试：灰度开关关闭走旧增量链路，开关开启走 dispatcher 新链路；replan 归档旧 session + 创建新 session 行为被覆盖。
- 更新编排运行指南与计划验收清单：`docs/guide/ORCHESTRATION_EVENT_DRIVEN_DISPATCHER_RUNTIME.MD` 增补收口说明，`docs/plan/ORCHESTRATION_EVENT_DRIVEN_PLANNER_CONTROL_PLAN.MD` 状态更新为完成并勾选验收项。
- 清理调度器类型安全：移除 `OrchestrationStepDispatcherService` 对 `IncrementalPlanningService` 的 `as any` 私有方法调用，改为显式 public 协作方法。
- 收敛状态窗口：`idle -> generating` 认领后直接使用认领状态推进 `phaseGenerate`，去除二次读取带来的额外竞态窗口。
- 增强 retry 保护：`retryCurrentTask` 增加 `maxRetries/maxTotalFailures` 前置限制，超限直接拒绝。
- 提升 pre/post planner 输出稳定性：Pre/Post prompt 增加 JSON-only 强约束前后缀。
- 事件总线注册层级调整：`EventEmitterModule.forRoot()` 上移到 `AppModule` 根模块。
- runtimeTaskType 遗留清理：移除 `phasePreExecute` 中 `inferRuntimeTaskTypeFromPlanContext` 的 `typeof` 防御分支，统一直接调用。
- 补齐推断单测：新增 `orchestration-context-infer-tasktype.spec.ts`，覆盖 research/development/general 路由与 existing override 保留。
- 清理历史残留：删除 planner prompt 中“发送邮件/外部动作”规则，移除 `extractEmailSendProof()` 死代码。
- 清理过时测试：删除 `task-classification.service.spec.ts`，并修复增量规划测试中对已删除 `taskType` 字段的引用。
- 新增 guide/plan 文档：补齐 `ORCHESTRATION_TASK_TYPE_ROUTING_CHAIN.MD`，新增 `ORCHESTRATION_RUNTIME_TASKTYPE_CLEANUP_PLAN.MD`。
- 输出验证链路重构：将任务类型语义校验从 `execute` 阶段移出，统一交由 `phasePostExecute` 的 planner 决策处理。
- 新增 planner 验证 skill：`TaskOutputValidationService` 增加 post-execute skill 规则构建，并在 `buildPostTaskContext` 中注入 `runtimeTaskType` + skill 规则。
- 扩展 post 决策契约：`PostExecutionDecision` 新增 `validation` 结构化字段（`passed/verdict/missing/ruleVersion`）。
- 新增仓库 skill 文档：`docs/skill/orchestration-runtime-task-out-validation.md`。
- 更新测试：重写 `task-output-validation.service.spec.ts` 以匹配精简后的服务职责。
- 新增本次改造文档：`docs/plan/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_SKILL_PLAN.MD`、`docs/guide/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_RUNTIME.MD`、`docs/technical/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_SKILL_DESIGN.MD`。
- requirementId 回填职责迁移：`tryBackfillRequirementId` 从 `IncrementalPlanningService` 移出，改为在 `OrchestrationStepDispatcherService.phasePostExecute` 且仅 `development.plan` 成功任务时执行。
- 场景优化统一收口：新增 `SceneOptimizationService.applyPostExecuteOptimizations(context)`，按 `planDomainType + runtimeTaskType + taskStatus + taskOutput` 匹配规则后执行优化动作。
- requirementId 回填进一步收口到 scene optimization 规则 `development-plan-requirement-id-backfill`，dispatcher 仅调用统一优化入口。
- 补齐 scene optimization 单测：新增 `scene-optimization.service.spec.ts`，覆盖命中、不命中、已有 requirementId 不覆盖场景。

**影响范围**

- 后端：`backend/src/modules/orchestration/services/incremental-planning.service.ts`。
- 后端：`backend/src/modules/orchestration/services/plan-management.service.ts`。
- 后端：`backend/src/modules/orchestration/services/orchestration-step-dispatcher.service.ts`。
- 后端：`backend/src/modules/orchestration/services/orchestration-context.service.ts`。
- 后端：`backend/src/app.module.ts`、`backend/src/modules/orchestration/orchestration.module.ts`。
- 测试：`backend/test/orchestration/orchestration-step-dispatcher.service.spec.ts`。
- 测试：`backend/test/orchestration/plan-management.service.spec.ts`。
- 文档：`docs/guide/ORCHESTRATION_EVENT_DRIVEN_DISPATCHER_RUNTIME.MD`、`docs/plan/ORCHESTRATION_EVENT_DRIVEN_PLANNER_CONTROL_PLAN.MD`。
- 后端：`backend/src/modules/orchestration/planner.service.ts`、`backend/src/modules/orchestration/services/task-output-validation.service.ts`。
- 测试：`backend/test/orchestration/orchestration-context-infer-tasktype.spec.ts`、`backend/test/orchestration/planner.incremental-redesign.spec.ts`、`backend/test/orchestration/incremental-planning.redesign-taskid.spec.ts`。
- 文档：`docs/guide/ORCHESTRATION_TASK_TYPE_ROUTING_CHAIN.MD`、`docs/plan/ORCHESTRATION_RUNTIME_TASKTYPE_CLEANUP_PLAN.MD`。
- 后端：`backend/src/modules/orchestration/services/orchestration-execution-engine.service.ts`、`backend/src/modules/orchestration/services/orchestration-context.service.ts`、`backend/src/modules/orchestration/services/orchestration-step-dispatcher.service.ts`。
- 后端：`backend/src/modules/orchestration/services/incremental-planning.service.ts`（移除 requirementId 回填逻辑）。
- 后端：`backend/src/modules/orchestration/services/scene-optimization.service.ts`（新增 post_execute 规则框架与 requirementId 回填规则）。
- 测试：`backend/test/orchestration/scene-optimization.service.spec.ts`。
- 文档：`docs/plan/ORCHESTRATION_SCENE_OPTIMIZATION_PIPELINE_PLAN.MD`、`docs/guide/ORCHESTRATION_SCENE_OPTIMIZATION_RUNTIME.MD`。
- 测试：`backend/test/orchestration/task-output-validation.service.spec.ts`。
- 规则资产：`docs/skill/orchestration-runtime-task-out-validation.md`。
- 文档：`docs/plan/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_SKILL_PLAN.MD`、`docs/guide/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_RUNTIME.MD`、`docs/technical/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_SKILL_DESIGN.MD`。

**关联文档**

- `docs/plan/ORCHESTRATION_EVENT_DRIVEN_PLANNER_CONTROL_PLAN.MD`
- `docs/guide/ORCHESTRATION_EVENT_DRIVEN_DISPATCHER_RUNTIME.MD`
- `docs/plan/ORCHESTRATION_RUNTIME_TASKTYPE_CLEANUP_PLAN.MD`
- `docs/guide/ORCHESTRATION_TASK_TYPE_ROUTING_CHAIN.MD`
- `docs/plan/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_SKILL_PLAN.MD`
- `docs/guide/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_RUNTIME.MD`
- `docs/technical/ORCHESTRATION_POST_EXECUTE_OUTPUT_VALIDATION_SKILL_DESIGN.MD`

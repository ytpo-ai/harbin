# 2026-03-30 工作日志

Planner Session 隔离模式开发：支持按 phase 拆分会话并保留 shared 兼容路径。

**完成事项**

- 新增环境变量 `PLANNER_SESSION_ISOLATION_MODE=shared`，支持 `shared|isolated` 切换。
- 扩展 `OrchestrationGenerationState`：新增可选字段 `plannerSessionIds?: Record<string, string>`，Schema 同步增加 `plannerSessionIds`。
- 改造 `orchestration-step-dispatcher.service.ts`：`ensurePlannerSession` 支持 phase 维度建会话，`advanceOnce` 按目标 phase 传递 session。
- 改造 session 回收逻辑：`archivePlannerSessionIfNeeded` 与 `stopGeneration` 在 isolated 模式下归档全部 phase session。
- `resolveGenerationState` 新增 `plannerSessionIds` 透传与归一化处理，避免阶段状态更新时丢失 phase session 映射。
- 补充并修正单测：新增 isolated 模式用例（phase-scoped session、stopGeneration 全量归档），并更新构造依赖与 post decision mock 字段。
- 验证通过：`backend` 下 `npm test -- orchestration-step-dispatcher.service.spec.ts`、`npm run build`。

**影响范围**

- 配置与数据模型：`backend/.env.example`、`backend/src/shared/schemas/orchestration-plan.schema.ts`。
- 编排调度核心：`backend/src/modules/orchestration/services/orchestration-step-dispatcher.service.ts`。
- 单元测试：`backend/test/orchestration/orchestration-step-dispatcher.service.spec.ts`。
- 文档：`docs/feature/ORCHETRATION_TASK.md`、`docs/guide/ORCHESTRATION_EVENT_DRIVEN_DISPATCHER_RUNTIME.MD`。

**关联文档**

- `docs/plan/PLANNER_SESSION_ISOLATION_MODE_PLAN.md`
- `docs/guide/ORCHESTRATION_EVENT_DRIVEN_DISPATCHER_RUNTIME.MD`

---

## Session 隔离后续修复：Skill 裁剪 + Prompt 阶段隔离 + Planner 纯文本 Retry

**完成事项**

- 修复 `phaseInitialize`/`phaseGenerate` 中 `plannerSessionId` 硬编码问题，改为统一使用 `withPlannerSession()` 方法写入 session state。
- 新增 `toolset-context.builder.ts` 中 `stripPhaseInitializeSectionIfNeeded()`：当 `roleInPlan` 以 `planner` 开头时，从 skill 文档中裁剪 `## phaseInitialize 行为` 段落，防止 system prompt 层级的指令误导 LLM。
- 在 4 个 planner 阶段的 user prompt 中注入阶段隔离声明：
  - generating（`planner.service.ts`）：声明当前阶段、禁止 phaseInitialize 指令、限定允许工具范围。
  - initialize（`planner.service.ts`）：反确认约束（"第一条回复必须是 tool_call"）。
  - pre_execute / post_execute（`orchestration-context.service.ts`）：阶段隔离声明。
- 新增 `agent-executor.service.ts` 中 `isPlannerTextOnlyRetryNeeded()`：当 planner 输出纯文本时注入系统纠正消息 retry 一次。
- 验证通过：generating 阶段 planner 正确调用 submit-task 生成任务；skill 裁剪生效；retry 机制正确触发。

**影响范围**

- 编排调度核心：`orchestration-step-dispatcher.service.ts`、`planner.service.ts`、`orchestration-context.service.ts`
- Agent runtime：`agent-executor.service.ts`、`toolset-context.builder.ts`
- Schema：`orchestration-plan.schema.ts`

**关联文档**

- `docs/issue/fix/2026-03-30-planner-session-isolation-and-skill-phase-stripping.md`
- `docs/plan/PLANNER_SESSION_ISOLATION_MODE_PLAN.md`
- `docs/issue/PLAN_OPTIMAZE.md`（#9 已完成，#18 待跟进）

---

## Agent Prompt 最小注入长度与可选化优化

**完成事项**

- 新增 Runtime Identity Layer 注入门槛：`systemPrompt` 在 `trim` 后长度小于 5 时，不注入 session context。
- 后端 Agent Schema 与共享类型将 `systemPrompt` 改为可选字段，保留创建时默认 prompt 兜底逻辑。
- 前端 Agent 管理编辑弹窗移除 Prompt 必填阻断，允许空 prompt 保存。
- 前端 Agent 管理“基础信息”中“能力集（逗号分隔）”字段移动到“描述”字段下方。
- 新增单测覆盖短 prompt 不注入场景；验证通过：`frontend npm run build`、`backend npm test -- identity-context.builder.spec.ts --runInBand`。

**影响范围**

- 后端 Runtime 上下文组装：`backend/apps/agents/src/modules/agents/context/identity-context.builder.ts`。
- 后端数据模型与类型：`backend/apps/agents/src/schemas/agent.schema.ts`、`backend/src/shared/types.ts`。
- 前端 Agent 管理：`frontend/src/components/agents/EditAgentModal.tsx`、`frontend/src/components/agents/CreateAgentModal.tsx`、`frontend/src/types/index.ts`。
- 测试：`backend/apps/agents/src/modules/agents/context/identity-context.builder.spec.ts`。
- 文档：`docs/feature/AGENT_RUNTIME.md`、`docs/feature/AGENT_MG.md`。

**关联文档**

- `docs/plan/AGENT_PROMPT_OPTIONAL_MIN_LENGTH_INJECTION_PLAN.md`

---

## Prompt 管理列表筛选状态持久化修复

**完成事项**

- 修复 Prompt 管理列表中“版本查询被 categoryTab 误过滤”问题，查询改为按 `scene + role + status` 拉取完整版本链路。
- 新增 `promptRegistryStore`（zustand + persist），持久化 `categoryTab/scene/role/statusFilter`，返回列表页后保持上次定位。
- 修正筛选联动：`scene` 变化后回填 `role` 改为使用当前作用域 `roleOptions`，避免跨分类选项污染。
- 验证通过：`frontend npm run build`。

**影响范围**

- 前端页面：`frontend/src/pages/PromptRegistry.tsx`。
- 前端状态：`frontend/src/stores/promptRegistryStore.ts`。

---

## Prompt 管理筛选持久化回归修复（Maximum update depth）

**完成事项**

- 修复 `PromptRegistry` 页面死循环更新：当筛选候选为空时仅在值发生变化时才回写 `scene/role`，避免重复 set 导致无限渲染。
- 修复 store setter 幂等性：`setScene/setRole/setCategoryTab/setStatusFilter` 在值未变化时不再触发状态更新。
- 验证通过：`frontend npm run build`。

**影响范围**

- 前端页面：`frontend/src/pages/PromptRegistry.tsx`。
- 前端状态：`frontend/src/stores/promptRegistryStore.ts`。

# 2026-03-29 工作日志

Orchestration 无用 MCP 工具清理：下线 5 个历史工具并完成 seed 同步。

**完成事项**

- 从工具目录移除以下 5 个 Orchestration MCP 工具：`complete-human-task`、`create-schedule`、`debug-task`、`reassign-task`、`update-schedule`。
- 移除工具分发路由，避免旧 toolId 继续被执行入口命中。
- 清理 Agent 常量与 legacy alias 映射，避免 profile/配置继续引用已下线工具。
- 调整 `mcp-profile` seed：`mode=sync` 改为覆盖写入 `tools`，避免历史工具残留。
- 将 5 个 canonical tool id 纳入 `DEPRECATED_TOOL_IDS`，配合 `builtin-tools` sync 执行数据清理。
- 执行 `npm run seed:manual -- --only=builtin-tools,mcp-profiles --mode=sync` 完成落库对齐。
- 补充开发总结文档并更新功能文档引用。

**影响范围**

- 后端工具注册与分发：`backend/apps/agents/src/modules/tools/builtin-tool-catalog.ts`、`backend/apps/agents/src/modules/tools/tool-execution-dispatcher.service.ts`、`backend/apps/agents/src/modules/tools/builtin-tool-definitions.ts`。
- Agent 工具常量与映射：`backend/apps/agents/src/modules/agents/agent.constants.ts`、`backend/apps/agents/src/modules/agents/agent-mcp-profile.service.ts`。
- Seed：`backend/scripts/seed/mcp-profile.ts`。
- 文档：`docs/development/ORCHESTRATION_UNUSED_MCP_TOOL_CLEANUP_2026-03-29.md`、`docs/feature/AGENT_TOOL.md`。

**关联文档**

- `docs/development/ORCHESTRATION_UNUSED_MCP_TOOL_CLEANUP_2026-03-29.md`
- `docs/feature/AGENT_TOOL.md`

---

Orchestration phaseInitialize 落地：需求选择前置到初始化阶段，并引入 plan/task 级 taskContext 自动注入。

**完成事项**

- `generationState.currentPhase` 新增 `initialize` 阶段，Dispatcher 在 `idle` 首次推进时优先进入初始化流程。
- 新增 `PlannerService.initializePlan()`：统一生成 requirement + outline 初始化结果，失败时回退默认大纲。
- Dispatcher 初始化阶段落库 `metadata.taskContext` 与 `metadata.outline`，development 场景强校验 requirementId。
- 删除 SceneOptimization 中基于执行输出正则回填 requirementId 的旧逻辑，改为 initialize 阶段一次性锚定。
- 执行态 prompt 新增“计划上下文（系统自动注入）”区块，task/run 均自动注入 taskContext。
- run 创建时快照 `metadata.taskContext`（手动 run + incremental autorun），保证运行追溯一致性。
- rd-workflow 升级到 v0.5.0：新增 phaseInitialize，步骤收敛为 step1-3。
- 执行验证：`backend` 下 `npm run build` 通过。

**影响范围**

- 后端编排核心：`backend/src/modules/orchestration/services/orchestration-step-dispatcher.service.ts`、`backend/src/modules/orchestration/planner.service.ts`、`backend/src/modules/orchestration/services/incremental-planning.service.ts`。
- 执行上下文与运行快照：`backend/src/modules/orchestration/services/orchestration-context.service.ts`、`backend/src/modules/orchestration/services/orchestration-execution-engine.service.ts`、`backend/src/modules/orchestration/services/plan-execution.service.ts`。
- 兼容数据结构：`backend/src/shared/schemas/orchestration-plan.schema.ts`。
- 旧逻辑清理：`backend/src/modules/orchestration/services/scene-optimization.service.ts`。
- 技能文档：`docs/skill/rd-workflow.md`。

**关联文档**

- `docs/plan/ORCHESTRATION_PHASE_INITIALIZE_PLANNER_REQUIREMENT_PLAN.md`
- `docs/skill/rd-workflow.md`

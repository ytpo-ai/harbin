# 2026-03-13 工作日志

**完成事项**

- 完成研发管理页交互优化：新增“先选择研发 Agent，再同步 OpenCode projects”的操作流。
- 后端复用 `rdproject` 并调整集合名为 `ei_projects`，新增同步来源字段与幂等索引。
- 新增 `POST /rd-management/agents/:agentId/opencode/projects/sync` 接口，按 Agent 维度批量同步并返回统计结果。
- 限制项目创建入口：`createProject` 改为仅允许同步链路创建，禁止前端手工创建 EI 项目。
- 前端 `RdManagement` 页面接入 Agent 列表筛选（`execution.provider=opencode`）和同步按钮，项目列表按 Agent 过滤。
- 二次收敛研发管理页面交互：移除前端创建 Session 与本地绑定入口，顶部统一为 Agent 选择 + EI Project 选择 + 同步按钮。
- 修复 Agent 项目同步筛选：按 `endpointRef` 拉取 OpenCode 项目并修正 scope 匹配规则，避免根路径 `/`（global）误命中。
- 修复 OpenCode sessions 拉取逻辑：同步链路改为按 `directory` 查询会话，避免把 global sessions 写入项目元数据；并支持 `/rd-management/opencode/sessions?directory=...`。

**影响范围**

- 后端：`backend/src/modules/rd-management/rd-management.service.ts`、`backend/src/modules/rd-management/rd-management.controller.ts`、`backend/src/modules/rd-management/rd-management.module.ts`
- 数据模型：`backend/src/shared/schemas/rd-project.schema.ts`
- 前端：`frontend/src/pages/RdManagement.tsx`、`frontend/src/services/rdManagementService.ts`
- 前端（二次调整）：`frontend/src/pages/RdManagement.tsx`
- 文档：`docs/plan/RD_MANAGEMENT_EI_PROJECT_SYNC_PLAN.md`、`docs/feature/ENGINEERING_INTELLIGENCE.md`、`docs/api/legacy-api.md`

**关联文档**

- 计划文档：`docs/plan/RD_MANAGEMENT_EI_PROJECT_SYNC_PLAN.md`

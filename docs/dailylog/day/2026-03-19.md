# 2026-03-19 工作日志

**完成事项**

- 完成 Inner Message 订阅路由 Redis 化改造：订阅创建/更新后同步写入 Redis 订阅主数据与索引。
- 新增事件定义注册表写入：发布和订阅写入时同步维护 `inner:event:def:*` 结构。
- 发布链路匹配优化：`publishMessage` 优先通过 Redis 索引（精确/域通配/全局）检索订阅，减少 Mongo 查询压力。
- 增加启动回灌机制：Redis 可用时自动从 Mongo 重建订阅索引与事件定义缓存。
- 保留降级路径：Redis 不可用时回退 Mongo 匹配，保障功能可用性。
- 新增事件定义查询接口：`GET /inner-message-subscriptions/event-definitions`，支持前端监听页动态加载事件目录。
- 新增订阅索引重建接口：`POST /inner-message-subscriptions/rebuild-index`，用于运维排障与索引纠偏。
- 消息中心“消息监听”Tab 改为动态事件目录：前端直连 `event-definitions` 接口加载事件选项，移除硬编码事件枚举。
- 修复会议结束流程幂等性：`endMeeting` 增加防重复结束触发保护。
- 新增技术设计文档，明确 Redis key 结构、流程与一致性策略。
- 执行后端构建验证通过（`pnpm -C backend run build`）。
- 执行前端构建验证通过（`pnpm -C frontend run build`）。

**影响范围**

- `backend/src/modules/inner-message/inner-message.service.ts`
- `backend/src/modules/inner-message/inner-message.controller.ts`
- `backend/libs/infra/src/redis.service.ts`
- `docs/technical/INNER_MESSAGE_REDIS_EVENT_SUBSCRIPTION_ROUTING_DESIGN.md`
- `docs/feature/INNER_MESSAGE.md`
- `docs/api/legacy-api.md`
- `frontend/src/pages/MessageCenter.tsx`
- `frontend/src/services/messageCenterService.ts`
- `docs/feature/MESSAGE_CENTER.md`
- `backend/src/modules/meetings/meeting.service.ts`
- `docs/feature/MEETING_CHAT.md`

**关联文档**

- `docs/technical/INNER_MESSAGE_REDIS_EVENT_SUBSCRIPTION_ROUTING_DESIGN.md`
- `docs/feature/INNER_MESSAGE.md`

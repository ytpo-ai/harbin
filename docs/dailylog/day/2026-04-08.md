# 2026-04-08 工作日志

补齐 Agent Session 级 token/cost 汇总展示能力，并提供历史数据回填脚本。

**完成事项**

- 在 `agent-session.schema.ts` 新增 `totalTokens`（input/output/reasoning/cacheRead/cacheWrite/total）与 `totalCost` 字段，默认值为 0。
- 在 `runtime-persistence.service.ts#createMessage` 增加 session 级 usage 增量写入：message 落库后按 `$inc` 原子累加到 `agent_sessions`。
- 新增 `runtime-persistence.service.spec.ts`，覆盖 session usage `$inc` payload 的构建逻辑（正常值、非有限值过滤、空输入）。
- 新增迁移脚本 `backend/scripts/migrate/backfill-session-token-cost.ts`，支持按 `sessionId` 聚合 `agent_messages` 回填 `agent_sessions`，并支持 `--dry-run`、`--reset-missing`。
- 在 `backend/package.json` 增加命令 `migrate:session-token-cost`。
- 前端 Session 页面接入汇总字段：`SessionTab` 列表卡片新增总 tokens 与总 cost 展示，`SessionDrawer` 基础信息新增 token 分项与总费用卡片。
- 更新功能文档 `docs/feature/AGENT_RUNTIME.md`，同步说明 session 聚合字段与回填脚本。

**影响范围**

- Agents Runtime 持久化链路：`backend/apps/agents/src/modules/runtime/runtime-persistence.service.ts`
- Agents Session 数据模型：`backend/apps/agents/src/schemas/agent-session.schema.ts`
- 迁移脚本：`backend/scripts/migrate/backfill-session-token-cost.ts`
- 后端脚本命令：`backend/package.json`
- 前端 Agent 详情页：`frontend/src/components/agent-detail/SessionTab.tsx`、`frontend/src/components/agent-detail/SessionDrawer.tsx`
- 功能文档：`docs/feature/AGENT_RUNTIME.md`

**关联文档**

- `docs/plan/SESSION_TOKEN_COST_DISPLAY_PLAN.md`

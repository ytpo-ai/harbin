# 2026-03-26 工作日志

Scheduler 重构收口：定时服务已统一为 Agent inner-message 投递模型，并补齐消息生命周期回写。

**完成事项**

- 新增 Scheduler 消息生命周期监控：发送后按 `messageId` 轮询 inner-message 状态（`sent/delivered/processing/processed/failed`），异步回写 `lastRun`。
- 重试策略定稿：Scheduler 不再做执行层重试，统一依赖 inner-message `maxAttempts` 与死信机制。
- 失败链路收口：消息生命周期失败/超时时写入 `schedule.deadLetters` 并触发告警 webhook。
- 历史查询增强：message-center 查询支持 `scheduleId/messageId/source` 过滤，Scheduler 历史页改读 inner-message 轨迹。
- 前端与文档对齐：移除 planId 绑定删除校验、更新 Scheduler 历史文案与 MCP/API 参数示例。
- 使用用户提供 Bearer Token 完成一次手动触发 E2E 验证：`/api/schedules/:id/trigger` 可成功受理，`lastRun` 在监控超时后自动回写失败并写入 dead-letter（发现 inner-message 状态长期停留 `sent`，需继续排查 dispatcher/runtime 消费链路）。

**影响范围**

- 后端（legacy）：`backend/src/modules/scheduler/`、`backend/src/modules/agents-client/agent-client.service.ts`。
- 后端（agents）：`backend/apps/agents/src/modules/message-center/` 查询过滤能力增强。
- 前端：`frontend/src/pages/Scheduler.tsx`、`frontend/src/pages/Orchestration.tsx`、`frontend/src/services/schedulerService.ts`。
- 文档：Scheduler feature/api/plan/technical/skill/dailylog 多文档同步到新语义。
- EI 服务拆分：按 `EI_SERVICE_SPLIT_PLAN` 将 `ei.service.ts` 拆分至 `repositories/requirements/statistics/opencode-sync` 四个领域服务，并新增 `ei-github-client.service.ts` 作为 GitHub 基础设施共享层。
- EI 模块装配更新：`app.module.ts` 移除 `EngineeringIntelligence` provider，改为领域服务直接承载逻辑并注册 `EiGithubClientService`。
- 文档对齐：同步更新 EI/Message Center 相关 feature/guide/technical 文档中的服务文件映射（`ei.service.ts` -> 拆分后领域服务）。

**关联文档**

- `docs/plan/SCHEDULER_SERVICE_REFACTOR_PLAN.md`
- `docs/technical/SCHEDULER_SERVICE_REFACTOR_TECHNICAL_DESIGN.md`
- `docs/feature/ORCHETRATION_SCHEDULER.md`
- `docs/api/agents-api.md`
- `docs/plan/EI_SERVICE_SPLIT_PLAN.md`
- `docs/guide/EI_SERVICE_SPLIT_RUNTIME.MD`
- `docs/feature/MESSAGE_CENTER.md`
- `docs/guide/MESSAGE_CENTER_CROSS_SERVICE_EVENT_STREAMS.MD`

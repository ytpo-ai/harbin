# 2026-03-26 工作日志

Scheduler 重构收口：定时服务已统一为 Agent inner-message 投递模型，并补齐消息生命周期回写。

**完成事项**

- 新增 Scheduler 消息生命周期监控：发送后按 `messageId` 轮询 inner-message 状态（`sent/delivered/processing/processed/failed`），异步回写 `lastRun`。
- 重试策略定稿：Scheduler 不再做执行层重试，统一依赖 inner-message `maxAttempts` 与死信机制。
- 失败链路收口：消息生命周期失败/超时时写入 `schedule.deadLetters` 并触发告警 webhook。
- 历史查询增强：message-center 查询支持 `scheduleId/messageId/source` 过滤，Scheduler 历史页改读 inner-message 轨迹。
- 前端与文档对齐：移除 planId 绑定删除校验、更新 Scheduler 历史文案与 MCP/API 参数示例。
- 使用用户提供 Bearer Token 完成一次手动触发 E2E 验证：`/api/schedules/:id/trigger` 可成功受理，`lastRun` 在监控超时后自动回写失败并写入 dead-letter（发现 inner-message 状态长期停留 `sent`，需继续排查 dispatcher/runtime 消费链路）。
- Agent 管理页拆分重构完成：`pages/Agents.tsx` 从单体页面拆分为页面编排层 + `components/agents` 组件目录，抽离 `CreateAgentModal`、`EditAgentModal`、`ModelTestPanel`、`AgentCard`、`AgentListHeader`。
- 共享逻辑下沉为 hooks/utils：新增 `useAgentListData`、`useAgentToolFilter`、`useAgentFormSync`，统一 Create/Edit 的角色联动与工具白名单过滤。
- 前端构建验证通过：`frontend` 执行 `npm run build` 成功。
- Orchestration 页面拆分重构完成：`frontend/src/pages/Orchestration.tsx` 改为兼容导出，核心实现迁移至 `frontend/src/pages/orchestration/index.tsx`。
- 新增编排页面 hooks 分层：`useOrchestrationQueries`、`useOrchestrationMutations`、`useTaskEditing`，将 query/mutation/任务编辑状态解耦。
- 新增 9 个 UI 子组件（PlanListTable/CreatePlanModal/PlanDetailDrawer/SettingsTab/HistoryTab/AddTaskModal/DependencyModal/RunDetailDrawer/DebugDrawer），移除单文件内联巨型 JSX。
- Orchestration 前端构建验证通过：`pnpm -C frontend build` 成功。
- Message Center 内部消息展示优化：发送方/接收方优先展示可读名称（Agent 名称/payload 显示名），并保留 ID 兜底信息。
- Message Center 内部消息新增“查看原始消息”能力：点击后打开右侧抽屉展示格式化 JSON，并支持复制消息 JSON，用于排障时快速核对入库字段。
- Meetings 页面拆分重构完成：`frontend/src/pages/Meetings.tsx` 改为兼容导出，主实现迁移至 `frontend/src/pages/meetings/index.tsx`。
- Meetings 查询/写操作/选择/实时订阅解耦为 hooks（`useMeetingQueries`、`useMeetingMutations`、`useMeetingSelection`、`useMeetingRealtime`），输入辅助拆分为 `useMentionAutocomplete`、`usePhraseAutocomplete`、`useMessageHistory`。
- Meetings UI 拆分为 9 个组件（含 `MeetingStatusActions` 配置驱动菜单、`ChatInput` 输入交互组件、`CreateMeetingModal` 独立弹窗）。
- Meetings 前端构建验证通过：`pnpm -C frontend build` 成功。

**影响范围**

- 后端（legacy）：`backend/src/modules/scheduler/`、`backend/src/modules/agents-client/agent-client.service.ts`。
- 后端（agents）：`backend/apps/agents/src/modules/message-center/` 查询过滤能力增强。
- 前端：`frontend/src/pages/Scheduler.tsx`、`frontend/src/pages/Orchestration.tsx`、`frontend/src/services/schedulerService.ts`。
- 文档：Scheduler feature/api/plan/technical/skill/dailylog 多文档同步到新语义。
- EI 服务拆分：按 `EI_SERVICE_SPLIT_PLAN` 将 `ei.service.ts` 拆分至 `repositories/requirements/statistics/opencode-sync` 四个领域服务，并新增 `ei-github-client.service.ts` 作为 GitHub 基础设施共享层。
- EI 模块装配更新：`app.module.ts` 移除 `EngineeringIntelligence` provider，改为领域服务直接承载逻辑并注册 `EiGithubClientService`。
- 文档对齐：同步更新 EI/Message Center 相关 feature/guide/technical 文档中的服务文件映射（`ei.service.ts` -> 拆分后领域服务）。
- 前端：`frontend/src/pages/Agents.tsx` 与 `frontend/src/components/agents/*`。
- 文档：新增 `docs/guide/AGENTS_PAGE_SPLIT_REFACTOR_ARCHITECTURE.MD`，更新 `docs/feature/AGENT_MG.md` 与 `docs/plan/AGENTS_PAGE_SPLIT_REFACTOR_PLAN.md`。
- 前端：`frontend/src/pages/orchestration/*`、`frontend/src/pages/Orchestration.tsx`。
- 文档：新增 `docs/guide/ORCHESTRATION_PAGE_SPLIT_REFACTOR_ARCHITECTURE.MD`，更新 `docs/feature/ORCHETRATION_TASK.md`。
- 前端：`frontend/src/pages/MessageCenter.tsx` 内部消息展示链路。
- 文档：更新 `docs/feature/MESSAGE_CENTER.md` 与当日日志。
- 前端：`frontend/src/pages/Meetings.tsx` 与 `frontend/src/pages/meetings/*`。
- 文档：新增 `docs/guide/MEETINGS_PAGE_SPLIT_REFACTOR_ARCHITECTURE.MD`，更新 `docs/feature/MEETING_CHAT.md`。

**关联文档**

- `docs/plan/SCHEDULER_SERVICE_REFACTOR_PLAN.md`
- `docs/technical/SCHEDULER_SERVICE_REFACTOR_TECHNICAL_DESIGN.md`
- `docs/feature/ORCHETRATION_SCHEDULER.md`
- `docs/api/agents-api.md`
- `docs/plan/EI_SERVICE_SPLIT_PLAN.md`
- `docs/guide/EI_SERVICE_SPLIT_RUNTIME.MD`
- `docs/feature/MESSAGE_CENTER.md`
- `docs/guide/MESSAGE_CENTER_CROSS_SERVICE_EVENT_STREAMS.MD`
- `docs/plan/AGENTS_PAGE_SPLIT_REFACTOR_PLAN.md`
- `docs/guide/AGENTS_PAGE_SPLIT_REFACTOR_ARCHITECTURE.MD`
- `docs/feature/AGENT_MG.md`
- `docs/plan/ORCHESTRATION_PAGE_SPLIT_REFACTOR_PLAN.md`
- `docs/guide/ORCHESTRATION_PAGE_SPLIT_REFACTOR_ARCHITECTURE.MD`
- `docs/feature/ORCHETRATION_TASK.md`
- `docs/feature/MESSAGE_CENTER.md`
- `docs/plan/MEETINGS_PAGE_SPLIT_REFACTOR_PLAN.md`
- `docs/guide/MEETINGS_PAGE_SPLIT_REFACTOR_ARCHITECTURE.MD`
- `docs/feature/MEETING_CHAT.md`

# 2026-04-30 工作日志

Discussion Space REQ-008：消息级知识落档能力（消息按钮 + 关联已有 + 新建并关联）。

**完成事项**

- 新增需求文档：`docs/requirement/DISCUSSION_SPACE_REQ-008_MESSAGE_KNOWLEDGE_ARCHIVE_ACTION.md`
- 更新计划与追溯：`docs/plan/DISCUSSION_SPACE_PLAN.md`、`docs/feature/DISCUSSION_SPACE.md`
- 后端新增消息-知识关联接口：`POST /discussions/:spaceId/threads/:threadId/messages/:messageId/knowledge/link`
- 后端新增消息关联校验：关联前校验 message 存在、知识条目存在且可用，并保持幂等关联
- 前端消息条目新增“落档知识库”入口，支持从消息触发落档动作
- 前端新增落档弹窗：支持“选择已有知识条目”与“新建知识条目”双模式
- 新建知识模式支持基于当前消息预填标题/摘要/内容，提交后自动关联到当前消息
- 完成验证：`backend` 执行 `pnpm run build:agents`、`pnpm run lint`；`frontend` 执行 `pnpm build`（无 `lint` 脚本）

**影响范围**

- 后端：`backend/src/modules/discussions/discussion.controller.ts`、`backend/src/modules/discussions/discussion.types.ts`、`backend/src/modules/discussions/services/discussion-knowledge.service.ts`
- 前端：`frontend/src/components/discussion/MessageBubble.tsx`、`frontend/src/components/discussion/KnowledgeArchiveModal.tsx`、`frontend/src/pages/discussions/DiscussionDetail.tsx`、`frontend/src/services/discussionService.ts`
- 文档：`docs/plan/`、`docs/requirement/`、`docs/feature/`、`docs/dailylog/day/`

**关联文档**

- `docs/plan/DISCUSSION_SPACE_PLAN.md`
- `docs/requirement/DISCUSSION_SPACE_REQ-008_MESSAGE_KNOWLEDGE_ARCHIVE_ACTION.md`
- `docs/feature/DISCUSSION_SPACE.md`

---

Discussion Space REQ-013（进行中）：讨论消息转需求 + 来源追溯。

**新增完成事项**

- Legacy 服务新增跨服务桥接：`discussion-requirement-bridge.service.ts`，通过签名头调用 EI `POST /ei/requirements`
- 讨论模块新增接口：`POST /discussions/:spaceId/threads/:threadId/messages/:messageId/to-requirement`
- EI `ei_requirements` 新增 `discussionSource` 字段，并在创建需求 DTO/Service 中完成写入
- 前端 `MessageBubble.tsx` 新增“转为需求”动作，`DiscussionDetail.tsx` 新增转需求弹窗与提交流程
- 前端 `EngineeringRequirementDetail.tsx` 新增“来源讨论”卡片，支持跳回来源讨论消息

**待完成事项**

- （已完成）知识面板 `action_item + metadata.isStructuredData=true` 条目快捷“转为数据采集需求”入口

---

Discussion Space REQ-014（进行中）：数据采集框架（EI）MVP。

**新增完成事项**

- EI 新增数据源 Schema：`ei_data_sources`（含 `collectFrequency/scheduleId/statistics` 等）
- EI 新增采集记录 Schema：`ei_data_records`（含 `data/dataCategory/tags/collectedAt/status` 等）
- 新增数据源 API：`/ei/data-sources`（CRUD + `test` + `collect`）
- 新增数据记录 API：`/ei/data-records`（列表、聚合、单删、批量清理）
- 创建数据源时自动创建 `orchestration_schedules` 并回写 `scheduleId`
- 前端新增 `DataDashboard.tsx`（数据源状态、手动采集、分类统计、最近记录）
- 孵化项目详情页新增“数据看板”入口与路由 `/ei/incubation/:id/data`
- 文档同步：新增开发记录 `docs/development/DISCUSSION_SPACE_REQ-014_DEVELOPMENT.md`，更新 requirement/feature/api 文档

**验证**

- `backend`：`pnpm run build:ei` 通过
- `frontend`：`pnpm run build` 通过

**未完成**

- 无（REQ-014 验收项已闭环）

**新增推进（继续开发）**

- EI 数据源创建新增 `executorAgentId` 要求，确保 schedule 可投递到指定 Agent
- schedule prompt 改造为工具驱动执行：先 `get-source-config` 再 `write-record`
- Agents 新增 MCP 工具实现：
  - `builtin.sys-mg.mcp.data-collection.get-source-config`
  - `builtin.sys-mg.mcp.data-collection.write-record`
- 工具分发链路已接入：`tool-execution-dispatcher` + `tool.module` + 内置工具目录
- 角色种子中为 `ai-data-analyst` 增加数据采集工具授权
- 验证补充：`backend` 执行 `pnpm run build:agents` 通过（此前 `build:ei` 已通过）

---

Discussion Space REQ-012（完成）：孵化项目与讨论空间聚合打通。

**新增完成事项**

- EI 聚合服务接入 `discussion_spaces`：新增 `getProjectDiscussionSpaces(projectId)`，支持项目级讨论空间列表查询
- EI 聚合统计新增 `discussions` 维度（`total` + `byCategory`）
- EI Controller 新增接口：`GET /ei/incubation-projects/:id/discussions`
- 前端 `IncubationProjectDetail.tsx` 新增“讨论空间”Tab 与讨论空间统计卡片
- 讨论空间卡片展示标题、分类、状态、消息数、知识条目数、更新时间，并支持点击跳转详情
- `SpaceCreateModal.tsx` 关联项目改为下拉选择（孵化项目列表）
- 修复讨论空间聚合返回 ID 兼容问题：后端统一返回 `id`，前端兼容 `id/_id`
- 新增单测：`incubation-project-aggregation.service.spec.ts`

**验证**

- `backend`：`pnpm --filter ei test -- services/incubation-project-aggregation.service.spec.ts` 通过
- `backend`：`pnpm --filter ei run build` 通过

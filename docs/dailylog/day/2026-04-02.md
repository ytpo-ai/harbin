# 2026-04-02 工作日志

Agent 工具按需 Schema Grounding 开发落地（get-tool-schema + authFree + 分层 preflight + 预激活透传）。

**完成事项**

- 在 `backend/apps/agents` 增加 `get-tool-schema` 元工具定义与分发执行逻辑，支持基于 `assignedToolIds` 的查询边界校验。
- 将 `search-memo`、`append-memo`、`send-internal-message`、`get-tool-schema` 标记为 `authFree`，并新增 `AUTH_FREE_TOOL_IDS`，`agent-role.service.ts` 改为动态合并默认工具。
- 扩展 `agent-executor`：新增 schema hint 生成、preactivated schema 注入、D1 分层扣分、D3 对 get-tool-schema 豁免。
- 在 orchestration 链路透传 `preactivatedToolIds`：`planner.service.ts` 按 phase 预置工具，`orchestration-step-dispatcher` 透传 preExecuteActions 工具，`orchestration-execution-engine` 支持任务级 requiredTools 透传。
- 升级 Composio 工具参数定义为标准 JSON Schema（SERP/Slack/Gmail），并补充单测。
- 更新功能文档：`docs/feature/AGENT_RUNTIME.md`、`docs/feature/AGENT_TOOL.md`。

**影响范围**

- Agent runtime 工具调用主链路（参数预检、扣分、schema 注入行为）。
- Tool catalog / seed 数据（新增 authFree 与 get-tool-schema，Composio schema 结构调整）。
- Orchestration planner sessionContext 透传字段（新增 `preactivatedToolIds`）。
- 工具分发单测与 executor helper 相关单测。

**关联文档**

- `docs/plan/AGENT_TOOL_ON_DEMAND_SCHEMA_GROUNDING_PLAN.md`
- `docs/feature/AGENT_RUNTIME.md`
- `docs/feature/AGENT_TOOL.md`

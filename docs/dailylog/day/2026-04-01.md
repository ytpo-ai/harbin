# 2026-04-01 工作日志

Agent 详情页日志列表精简与 Run 扣分记录对接。

**完成事项**

- 前端服务层新增 Run 评分类型与 `getRunScore(runId)` 调用，404 返回 `null`。
- Agent 详情日志状态 Hook 支持详情 Tab 状态管理、评分懒加载与缓存。
- 日志列表折叠态改为单行摘要（状态、评分、耗时、环境），移除事件数与末条摘要噪音。
- 展开态重构为「执行流程 / 原始信息 / 扣分记录」三 Tab；扣分记录新增概览卡、规则汇总、可折叠明细。
- 更新 Runtime 功能文档，补充日志 Tab 与评分对接说明。
- 按 v2 方案新增后端 `GET /agents/runtime/runs` 列表端点，并补齐 Runtime `getRun()` 返回字段（`taskTitle/agentName/metadata/score`）。
- 日志 Tab 数据源从 `agent-action-logs` 客户端聚合切换为 `agent_runs` 直接分页查询，筛选栏移除 `contextType`。

**影响范围**

- 前端：`frontend/src/services/agentService.ts`
- 前端：`frontend/src/components/agent-detail/constants.ts`
- 前端：`frontend/src/components/agent-detail/hooks/useLogState.ts`
- 前端：`frontend/src/components/agent-detail/LogTab.tsx`
- 后端：`backend/apps/agents/src/modules/runtime/runtime-persistence.service.ts`
- 后端：`backend/apps/agents/src/modules/runtime/runtime-orchestrator.service.ts`
- 后端：`backend/apps/agents/src/modules/runtime/runtime.controller.ts`
- 文档：`docs/feature/AGENT_RUNTIME.md`

**关联文档**

- 计划文档：`docs/plan/AGENT_DETAIL_LOG_SCORE_INTEGRATION_PLAN.md`
